<rml>
<head>
    <title>Raid Frames</title>
    <link type="text/rcss" href="../overlay.rcss"/>
    <link type="text/rcss" href="../elements/bars.rcss"/>
    <style>
        body {
            position: absolute;
            left: 20dp;
            top: 150dp;
            width: 250dp;
            height: 400dp;
        }
        .raid-container {
            display: flex;
            flex-wrap: wrap;
            width: 240dp;
            gap: 4dp;
        }
        .raid-member {
            width: 55dp;
            height: 40dp;
            background-color: rgba(16, 16, 16, 0.95);
            border-width: 1dp;
            border-style: solid;
            border-color: #2d2d2d;
            border-radius: 0dp; /* Sharp corners */
            position: relative;
            overflow: hidden;
        }
        .raid-member-dead {
            border-color: #801010;
            background-color: rgba(48, 12, 12, 0.95);
        }
        .member-out-of-range {
            opacity: 0.55;
        }
        .raid-hp {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            background-color: #29a744; /* ElvUI vibrant green */
        }
        .raid-name {
            position: absolute;
            top: 4dp;
            width: 100%;
            text-align: center;
            font-size: 9dp;
            font-weight: bold;
            color: #ffffff;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }
        .raid-status-text {
            position: absolute;
            bottom: 4dp;
            width: 100%;
            text-align: center;
            font-size: 8dp;
            font-weight: bold;
            color: #ff3333;
        }
    </style>
</head>
<body data-model="wow">
    <!-- Show raid frames only when logged in and in a raid -->
    <div class="raid-container" data-show="playerIsIngame && numRaidMembers > 0">
        <div class="raid-member" data-for="member : raidMembers" data-class-raid-member-dead="member.health == 0">
            <!-- Health Fill -->
            <div class="raid-hp" data-hide="member.health == 0" style="height: {{ member.maxHealth > 0 ? (member.health * 100 / member.maxHealth) : 0 }}%;"></div>

            <!-- Member Name -->
            <div class="raid-name">{{member.name}}</div>

            <!-- Dead Status Text -->
            <div class="raid-status-text" data-show="member.health == 0">DEAD</div>
        </div>
    </div>
</body>
</rml>
